# Data — never commit the warehouse, raw dumps, or generated charts
data/*.duckdb
data/*.duckdb.wal
data/**/*.parquet
data/charts/
*.duckdb
*.duckdb.wal
# Exception: the prebuilt read-only warehouse is committed so the deployed
# API (Render) has data without a separate pipeline run. Keep it small.
!data/wc2026.duckdb

# Python
__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/
build/
dist/
.pytest_cache/
.ruff_cache/

# dbt
dbt/target/
dbt/dbt_packages/
dbt/logs/

# Dagster
.dagster_home/
tmp*/

# seleniumbase / scraper scratch artifacts
downloaded_files/
latest_logs/
assets/
geckodriver.log

# Environment
.env

# OS / IDE
.DS_Store
.vscode/
.idea/
